cert-manager:
  installCRDs: true
  prometheus:
    enabled: true
    servicemonitor:
      enabled: true

acme:
  # Email address for Let's Encrypt registration (required)
  email: "me@le-g.dev"
  # Zone ID is explicitly set in the ClusterIssuer template for le-g.dev
