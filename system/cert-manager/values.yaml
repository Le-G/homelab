cert-manager:
  installCRDs: true
  prometheus:
    enabled: true
    servicemonitor:
      enabled: true

acme:
  # Email address for Let's Encrypt registration (required)
  email: "me@le-g.dev"
  # Note: zoneID is not supported in cert-manager v1.11.2
  # The API token will automatically discover the zone
