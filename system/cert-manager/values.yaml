cert-manager:
  installCRDs: true
  prometheus:
    enabled: true
    servicemonitor:
      enabled: true

acme:
  # Email address for Let's Encrypt registration (required)
  email: "me@le-g.dev"
  cloudflare:
    # Cloudflare Zone ID (optional but recommended for faster certificate renewal)
    # Find it in Cloudflare dashboard or via: terraform output -json | jq -r '.cloudflare_zone_id.value'
    zoneID: "9a86bded0d2099191967bd93db8114bb"
